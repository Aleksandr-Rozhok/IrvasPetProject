import { showPopup } from "../services/services";

function calc() {
    const popupCalcButton = document.querySelectorAll('.popup_calc_btn');

    popupCalcButton.forEach(button => button.addEventListener('click', ))
}

export default calc;